# MySQL / TypeORM 접속 정보 템플릿
# 이 파일을 복사해 .env 를 만든 뒤 값을 채우세요: cp .env.example .env
DB_HOST=localhost
DB_PORT=3306
DB_USER=appuser
DB_PASSWORD=apppass
DB_NAME=appdb

# docker-compose의 MySQL root 비밀번호
DB_ROOT_PASSWORD=rootpass
